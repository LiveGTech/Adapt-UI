# `Section` component
Astronaut:
```javascript
Section() ()
```

HTML:
```html
<section></section>
```

An area inside a [`Page`](page.md) that can contain textual information and user interface components.